{% extends 'base.html.twig' %}

{% block title %}Edit Actu{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-9">
            <h3>Modifier une actualité</h3>

            {{ include('actu/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}

            <a href="{{ path('actu_index') }}">
                <button class="btn btn-secondary">Retour à la liste des produits</button>
            </a>

            {{ include('actu/_delete_form.html.twig') }}
        </div>
    </div>
</div>

{% endblock %}
