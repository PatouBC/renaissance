<form method="post" action="{{ path('day_part_delete', {'id': day_part.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce rendez-vous?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ day_part.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
