<form method="post" action="{{ path('day_part_type_delete', {'id': day_part_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ day_part_type.id) }}">
    <button class="btn">Delete</button>
</form>
