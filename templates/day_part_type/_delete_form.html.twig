<form method="post" action="{{ path('day_part_type_delete', {'id': day_part_type.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce créneau?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ day_part_type.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
