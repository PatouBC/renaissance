{% extends 'base.html.twig' %}

{% block title %}Créneau à modifier{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-9">
                <h3>Créneau à modifier</h3>

                {{ include('day_part_type/_form.html.twig', {'button_label': 'Enregistrer'}) }}

                <a href="{{ path('day_part_type_index') }}">
                    <button class="btn btn-secondary">Retour à la liste des catégories</button>
                </a>

                {{ include('day_part_type/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
