{% extends 'base.html.twig' %}

{% block title %}DayPartType{% endblock %}

{% block body %}
    <h1>DayPartType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ day_part_type.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ day_part_type.value }}</td>
            </tr>
            <tr>
                <th>Definition</th>
                <td>{{ day_part_type.definition }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('day_part_type_index') }}">back to list</a>

    <a href="{{ path('day_part_type_edit', {'id': day_part_type.id}) }}">edit</a>

    {{ include('day_part_type/_delete_form.html.twig') }}
{% endblock %}
