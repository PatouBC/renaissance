{% extends 'base.html.twig' %}

{% block title %}Daypart{% endblock %}

{% block body %}
    <h1>Daypart</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ daypart.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('daypart_index') }}">back to list</a>

    <a href="{{ path('daypart_edit', {'id': daypart.id}) }}">edit</a>

    {{ include('daypart/_delete_form.html.twig') }}
{% endblock %}
