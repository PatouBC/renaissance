{% extends 'base.html.twig' %}

{% block title %}Dayparttype{% endblock %}

{% block body %}
    <h1>Dayparttype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dayparttype.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ dayparttype.value }}</td>
            </tr>
            <tr>
                <th>Definition</th>
                <td>{{ dayparttype.definition }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dayparttype_index') }}">back to list</a>

    <a href="{{ path('dayparttype_edit', {'id': dayparttype.id}) }}">edit</a>

    {{ include('dayparttype/_delete_form.html.twig') }}
{% endblock %}
