{% extends 'base.html.twig' %}

{% block title %}Nouveau message{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-9">
                <h3>Créer un nouveau message</h3>

                {{ include('email/_form.html.twig') }}

                <a href="{{ path('email_index') }}">back to list</a>
            </div>
        </div>
    </div>

{% endblock %}
