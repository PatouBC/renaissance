{% extends 'base.html.twig' %}

{% block title %}Modifier une indication{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-9">
                <h3>Modifier une indication</h3>

                {{ include('indication/_form.html.twig', {'button_label': 'Modifier'}) }}

                <a href="{{ path('indication_index') }}">
                    <button class="btn btn-secondary">Retour à la liste des indications</button>
                </a>

                {{ include('indication/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
