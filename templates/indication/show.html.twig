{% extends 'base.html.twig' %}

{% block title %}Indication{% endblock %}

{% block body %}
    <h1>Indication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ indication.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ indication.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('indication_index') }}">back to list</a>

    <a href="{{ path('indication_edit', {'id': indication.id}) }}">edit</a>

    {{ include('indication/_delete_form.html.twig') }}
{% endblock %}
