{% extends 'base.html.twig' %}

{% block title %}Message à traiter{% endblock %}

{% block body %}
    <div class="container-fluid fond">
        <div class="row justify-content-center">
            <div class="col-9 edit fond2" style="margin: 20px">
                <h3>Message à traiter</h3>
                <div style="margin: 20px 0 20px 0">
                    {{ include('message/_form.html.twig', {'button_label': 'Modifier'}) }}
                </div>
                    <div class="row action">
                        {{ include('message/_delete_form.html.twig') }}
                    </div>
                    <div style="margin: 20px 0 20px 0" class="row action">
                        <a href="{{ path('message_index') }}">
                            <button class="btn btn-secondary">Annuler</button>
                        </a>
                    </div>



            </div>
        </div>
    </div>
{% endblock %}
