{% extends 'base.html.twig' %}

{% block title %}Message à traiter{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-9 edit">
                <h3>Message à traiter</h3>

                {{ include('message/_form.html.twig', {'button_label': 'Enregistrer'}) }}
                <div class="row justify-content-between bouton">
                    <div>
                        {{ include('message/_delete_form.html.twig') }}
                    </div>
                    <div>
                        <a href="{{ path('message_index') }}">
                            <button class="btn btn-secondary">Retour à la liste des messages</button>
                        </a>
                    </div>

                </div>

            </div>
        </div>
    </div>
{% endblock %}
