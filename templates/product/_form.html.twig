{{ form_start(form) }}
    <div class="justify-content-center">
        <div>
            <h6>Nom</h6>
        </div>
        <div>
            {{ form_widget(form.name)}}
        </div>
        <div>
             <h6>Catégorie</h6>
        </div>
        <div>
            {{ form(form.category)}}
        </div>
        <div>
            <h6>Etat émotionnel</h6>
        </div>
        <div>
            {{ form_widget(form.state)}}
        </div>
        <div>
            <h6>Effets</h6>
        </div>
        <div>
            {{ form_widget(form.effect)}}
        </div>
        <div>
            <h6>Famille d'émotions</h6>
        </div>
        <div>
            {{ form_widget(form.indications)}}
        </div>
        {{ form_rest(form) }}
        <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
