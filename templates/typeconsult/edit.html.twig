{% extends 'base.html.twig' %}

{% block title %}Modifier un type de consultation{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-9">
    <h3>Modifier un type de consultation</h3>

    {{ include('typeconsult/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('typeconsult_index') }}">
        <button class="btn btn-secondary">Annuler</button>
    </a>

    {{ include('typeconsult/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
