{% extends 'base.html.twig' %}

{% block title %}Typeconsult{% endblock %}

{% block body %}
    <h1>Typeconsult</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeconsult.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ typeconsult.description }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ typeconsult.rate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('typeconsult_index') }}">back to list</a>

    <a href="{{ path('typeconsult_edit', {'id': typeconsult.id}) }}">edit</a>

    {{ include('typeconsult/_delete_form.html.twig') }}
{% endblock %}
