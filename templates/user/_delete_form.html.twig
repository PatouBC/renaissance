<form method="post" action="{{ path('typeconsult_delete', {'id': typeconsult.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typeconsult.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
