{% extends 'base.html.twig' %}

{% block title %}Modifier une disponibilité{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-9">
                <h3>Modifier une disponibilité</h3>

                {{ include('workingday/_form.html.twig', {'button_label': 'Modifier'}) }}

                <a href="{{ path('workingday_index') }}">
                    <button class="btn btn-secondary">Retour aux disponibilités</button>
                </a>

                {{ include('workingday/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
